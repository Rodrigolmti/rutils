name: rutils_image
description: RUtils image package
version: 0.0.2+1

environment:
  sdk: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  permission_handler: ^8.1.4+2
  image_picker: ^0.8.3+3
  image_cropper: ^1.4.1
  path_provider: ^2.0.2

dev_dependencies:
  lints: ^1.0.0
