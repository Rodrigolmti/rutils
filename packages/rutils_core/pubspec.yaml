name: rutils_core
description: RUtils core package
version: 0.0.2+1

environment:
  sdk: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  device_info: ^2.0.3
  easy_mask: ^2.0.1
  intl: ^0.20.2
