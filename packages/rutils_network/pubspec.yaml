name: rutils_network
description: RUtils network package
version: 0.0.2+1

environment:
  sdk: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  internet_connection_checker: ^0.0.1+2
  dio: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^0.1.4
  lints: ^1.0.0
