name: rutils_example
description: A rutils_example project

publish_to: "none"

version: 0.0.1+1

environment:
  sdk: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  rutils_uikit:
    path: ../rutils_uikit

flutter:
  uses-material-design: true
